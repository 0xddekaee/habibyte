[package]
name = "habibyte-ledger"
version = "0.1.0"
edition = "2021"

[dependencies]
habibyte-identity = { path = "../habibyte-identity" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
hex = "0.4"
chrono = { version = "0.4", features = ["serde"] }
